import TimeSlot from "./TimeSlot";

function GroupAvailability({ timeSlots }: { timeSlots: any }) {
  return <div className="font-medium">Show group's availability here</div>;
}
export default GroupAvailability;
